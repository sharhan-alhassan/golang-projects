
# Setup: Getting started
```sh
- go mod init example/hello

# import an external module and use a function from it
import "rsc.io/quote/v4"

# Install the external module
- go mod tidy

# Run the app
go run hello.py     # or go mod .
```